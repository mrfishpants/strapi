'use strict';

/**
 * careers-page-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::careers-page-setting.careers-page-setting');